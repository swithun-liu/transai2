CREATE TABLE wordTranslation (
    bookPath TEXT NOT NULL,
    paragraphId INTEGER NOT NULL,
    word TEXT NOT NULL,
    context TEXT NOT NULL,
    translation TEXT NOT NULL,
    pronunciation TEXT NOT NULL,
    PRIMARY KEY (bookPath, paragraphId, word)
);
